# Continue here — Phase 1 planning, paused 2026-09-20 ~00:45

## Where we are

`/gsd-plan-phase 1` is **mid revision loop**, iteration 1 of 3.

Flow so far: research → context amendment → pattern mapping → planning → plan check → **revision (in progress when the session ended)**.

| Artifact | State | Commit |
|---|---|---|
| `.planning/phases/01-foundation/01-RESEARCH.md` | done (905 lines) | `2125607` |
| `.planning/phases/01-foundation/01-CONTEXT.md` | amended with research findings C-1…C-7 | `95a13db` |
| `.planning/phases/01-foundation/01-PATTERNS.md` | done (19 files classified, 11 analogs) | with `d634946` |
| `.planning/phases/01-foundation/01-0{1..7}-PLAN.md` | 7 plans, 4 waves | `d634946`, **revision edits may be uncommitted — see below** |
| `.planning/phases/01-foundation/COVERAGE.md` | API coverage matrix | with `d634946` |

## Decisions the user made this session (already applied, do not re-ask)

1. **Accepted all seven research-vs-locked-decision conflicts (C-1…C-7)** and had them written into CONTEXT.md: D-11 (Ed25519 tenant CA by choice, not constraint — AXIAM *can* generate RSA-4096), D-20 (REST management routes reject `aud: axiam:m2m`, so Phase 2 needs a tenant *user* principal), D-23 (no SDK device-login method — hand-roll `POST /api/v1/auth/device`), D-24 (broker-side `mqtt.ssl_cert_client_id_from = distinguished_name` makes the strong binding achievable), D-26 (broker CA bundle = org root, not tenant CAs), new **D-37** (AXIAM's own AMQP client cert is offline root-signed infrastructure; `domo-bootstrap` needs a per-tenant admin user because the Rust SDK never sends `X-Axiam-Tenant`), and the Claude's-Discretion JWT line (Twin uses the SDK's `token::JwksVerifier`, not a gRPC wrapper).
2. **MQTT topic `{device}` segment = the device's service-account UUID** — same value as cert CN, JWT `sub` and MQTT username, so the auth backend needs no registry lookup.

## Plan set (as committed in `d634946`)

| Plan | Wave | Depends on | Autonomous | Focus |
|---|---|---|---|---|
| 01-01 | 1 | — | **false** | Tracer: root → BYOK → tenant CA → device cert → mTLS login → MQTT CONNECT + pub/sub, plus repo floor |
| 01-02 | 2 | 01-01 | true | PKI verification suite, `just export-trust`, `docs/trust.md`, secrets guard |
| 01-03 | 2 | 01-01 | true | Caddy single origin, AXIAM console host, landing page, PostgreSQL 17 |
| 01-04 | 2 | 01-01 | true | `authz/catalog.toml`, tenant admin principal, both tenants + signing CAs, tree helper |
| 01-05 | 2 | 01-01 | true | Twin's four-endpoint RabbitMQ auth contract, JWKS verifiers, offline tests |
| 01-06 | 3 | 01-04, 01-05 | true | Smoke branch, live authz assertions, 10-case positive/negative connect matrix |
| 01-07 | 4 | 01-02, 01-03, 01-06 | true | Preflight, staged resumable checklist, `demo-reset`, arm64, `phase-verify`, demo card |

## What the revision was fixing (checker: 1 blocker, 6 warnings, 2 info)

The checker passed the design outright — architecture, tracer, identity chain, authz model, threat models, requirement coverage (14/14), decision coverage (D-01…D-37), wave-2 file disjointness. The findings are ordering/instruction-completeness only:

- **BLOCKER** — 01-06 Task 3 verifies with `just demo-reset`, but that recipe is first created by 01-07 (wave 4). 01-06 is wave 3 and cannot depend on 01-07 without a cycle.
- **W1** — 01-03 consumes 01-02's `dist/trust/` and `just verify-pki` with no declared edge; both are wave 2 and run concurrently. Also both touch `deploy/pki/listeners.conf`.
- **W2** — 01-06 verifies `just smoke-tree` / `just smoke-authz`; no task action instructs creating those recipes.
- **W3** — 01-07 Task 1 verifies with `DOMO_FAIL_AT=catalog`; no action instructs building that fault-injection hook.
- **W4** — 01-04 compiles Rust with no `df -h` check and no cleanup; no wave-2 plan asserts the 25 GB no-parallel-builds precondition (01-04 and 01-05 share one `target/`).
- **W5** — 01-01 is over budget: 35 files, 33 in one task. Fix is to relocate non-chain work (`Dockerfile.rust`, `.env.example`/dispatcher scaffolding, the findings log) — **not** to split the tracer chain.
- **W6** — `01-RESEARCH.md` § Open Questions lacks resolution markers; all five are in fact answered (Q1→the CONTEXT amendment, Q2→D-23, Q3→D-37 + 01-04 Task 2, Q4→deferred to Phase 3, Q5→human-check blocks).

## Resume

```
/clear
cd /home/emanuele/git/priv/axiam-domo-demo
git status            # confirm whether the revision edits are committed or still WIP
/gsd-plan-phase 1
```

**First thing to check on resume:** whether the reviser's edits to all 7 plans landed in a commit. If they are a WIP commit or uncommitted, re-run the plan checker against the current plans (revision iteration 2 of 3) rather than re-planning — the design was already validated, only the seven findings above were in flight. If the plans look half-edited, `git checkout .planning/phases/01-foundation/` restores the clean `d634946` plan set and the revision can be re-run from the finding list above.

After the checker passes: requirements coverage gate → decision coverage gate → `state.planned-phase` → ROADMAP wave annotations → commit → execute with `/gsd-execute-phase 1`.

## Known items needing the user, before or during execution

- **01-01 is `autonomous: false`**: the package-legitimacy gate rates `axiam-sdk` `[SUS]` (678 downloads, pre-release line only). It is first-party, so the decision is only how to depend on it — pin `=1.0.0-beta16`, or a `path` dependency to the local checkout. Needed before the first `Cargo.toml` is written.
- **`user_setup` items Claude cannot do**: importing the root into OS/Firefox/Chromium trust stores (sudo + GUI); publishing `domo.local` and `axiam.domo.local` via Avahi or `/etc/hosts` (research confirmed `domo.local` does **not** resolve on the XPS as of 2026-09-19); running `just up` on the actual Raspberry Pi 5; one-time outbound pull from ghcr.io.
- **Environment note**: `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` is enabled. GSD's multi-agent orchestration is not validated under it (open-gsd/gsd-core#1355) — subagent completions can fail to route back. Everything routed correctly this session, but it is the first thing to suspect if a stage appears to hang.
- Disk at pause: 35 GB free on `/home`, 44 GB on `/`. No builds were run.
